<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DarkSoulsAPP" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color.
        <item name="colorPrimary">@color/browndarksouls</item>
        <item name="colorPrimaryVariant">@color/browndarksouls</item>
        <item name="colorOnPrimary">@color/browndarksouls2</item>
        -->

        <!-- Status bar color.
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        -->
        <!-- Customize your theme here. -->

    </style>

    <!--Splash Screen
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/darksoulslogo</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.DarkSoulsAPP</item>
    </style>
    -->
</resources>